<% breadcrumb :edit_plot, @story, @plot %>
<div class="px-4 sm:px-6 lg:px-8 py-8">
  <div class="max-w-3xl mx-auto">
    <div class="p-8">
      <%= render 'form', story: @story, plot: @plot %>
    </div>
  </div>
</div>
